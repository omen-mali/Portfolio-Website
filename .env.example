# Set a password to lock the site behind a login page.
# Remove or leave empty to disable password protection.
SITE_PASSWORD=

# High-entropy secret used as the value of the access cookie (this is NOT the
# password). Generate with: openssl rand -hex 32
# BOTH SITE_PASSWORD and SITE_AUTH_TOKEN must be set for the gate to activate.
SITE_AUTH_TOKEN=
